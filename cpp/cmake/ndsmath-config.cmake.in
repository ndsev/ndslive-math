@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# ndsmath's public headers include GLM, so consumers need it too.
find_dependency(glm CONFIG)

include("${CMAKE_CURRENT_LIST_DIR}/ndsmath-targets.cmake")

check_required_components(ndsmath)
